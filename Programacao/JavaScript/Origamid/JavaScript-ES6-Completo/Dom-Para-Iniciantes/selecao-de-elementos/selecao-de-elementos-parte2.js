// getElementsByClass name = Atualiza em tempo real novos valores

// querySelectorAll = não atualiza em tempo real os valores.

// adicionar uma classe a um elemento
// element.classList.add('ul');

/*
  Array-like:
    HTMLCollection  e NodeList são array-like, parecem um array mas não são. O método do Array forEach() por exemplo, existe apenas em NodeList
    é possive transformar um Array-like em array normal utilizando o método Array.from(nomearray-like);
*/