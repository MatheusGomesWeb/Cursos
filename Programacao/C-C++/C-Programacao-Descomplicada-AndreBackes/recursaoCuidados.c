#include <stdio.h>

/*
    * Função Recursiva Cuidados:
    * Precisa sempre ter um critério de parada (if) para não entrar em looping infinito
    * o parametro de chamada recursiva precisa ter o valor mudado para não entrar em looping infinito: recursiva(N -1);
 */

int main() {

    return 0;
}